{
  "branches": ["main"],
  "plugins": [
    [
      "@semantic-release/commit-analyzer",
      {
        "preset": "conventionalcommits"
      }
    ],
    "@semantic-release/release-notes-generator",
    "@semantic-release/changelog",
    [
      "@semantic-release/exec",
      {
        "prepareCmd": "echo Releasing ${nextRelease.version} && npm version ${nextRelease.version} --no-git-tag-version --prefix api && npm version ${nextRelease.version} --no-git-tag-version --prefix web && sed -i -E '/^[[]project[]]$/,/^[[]/{s/^version = \".*\"/version = \"'${nextRelease.version}'\"/}' sdk/python/pyproject.toml",
        "publishCmd": "python -m build sdk/python --outdir sdk/python/dist && python -m twine upload sdk/python/dist/*"
      }
    ],
    [
      "@semantic-release/git",
      {
        "assets": [
          "CHANGELOG.md",
          "api/package.json",
          "api/package-lock.json",
          "web/package.json",
          "web/package-lock.json",
          "sdk/python/pyproject.toml"
        ]
      }
    ],
    "@semantic-release/github"
  ]
}
